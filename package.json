{
  "name": "meta-scheduler",
  "version": "0.2.0-pre",
  "description": "Persistent-agent + vault scaffolding for Claude Code. Currently ships only the vault layout (private/) and skills/; v2 supervisor and dashboard land here later.",
  "type": "module",
  "scripts": {
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
